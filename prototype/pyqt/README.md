# PyQt Prototype

This folder is the target home for the existing PyQt prototype.

Move these files here when you are ready:

- `app.py`
- `aero_music_player.py`
- `controller.py`
- `requirements.txt`
- `models/`
- `player/`
- `utils/`

They were left in place at the workspace root to avoid breaking the current runnable prototype.
